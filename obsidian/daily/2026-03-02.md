# 2026-03-02

## What Was Built Today
- **Voice Quality**: Changed TTS from "Kore" to "Aoede" (breezy/warm), added style prompt prefix for caring nurse tone, extracted to constants
- **Patient Onboarding**: Full flow — `create_minimal_patient()`, `save_patient()`, `list_patient_ids()` in data_loader; `POST /api/patients` endpoint; dynamic patient listing (no more hardcoded dict); `OnboardingForm` React component; "New Patient" option in both PatientView and ClinicianView selectors; `isOnboarding` state in IrisContext
- **GDMT Engine Fix**: EF=0.0 now treated as "unknown" instead of triggering beta blocker initiation
- **Image Intake**: `api/services/image.py` with Gemini multimodal extraction; `process_image_message()` in pipeline_service; image_data/image_mime_type fields in ChatRequest; camera button in both views with file picker, base64 encoding, preview chip
- **Mobile/iPad Responsive**: `useMediaQuery` hook with `useIsMobile()`/`useIsTablet()`/`useIsDesktop()`; ClinicianView now has 3 layouts — 3-col desktop, 2-col tablet, tabbed mobile
- **Patient Education**: `educationContent.ts` with plain language explanations for K+, Cr, eGFR, BNP, Na+, EF, NYHA; `MetricTooltip` component; wrapped dashboard metrics; enhanced responder prompt with educational instructions
- **UI Polish**: Theme-aware colors in ActionPacketCard and EscalationAlert (no more hardcoded #ccc/#eee/#ddd); standardized font family via CSS custom property; tightened padding from 16px to 12px

## Key Decisions Made
- Kept 5 demo patients intact, onboarding creates P006+ alongside them
- EF=0.0 is clinically implausible — treated as "not measured/unknown" in beta blocker logic
- Image intake uses same pipeline as text/audio — signals merge when both text and image provided
- Mobile ClinicianView uses tabs (Chat/Patient/Clinical) instead of trying to squeeze 3 columns
- Education tooltips appear on hover (desktop) and tap (mobile)

## Problems Encountered
- Duplicate `retryPatients` destructuring in ClinicianView after adding onboarding — fixed during responsive rewrite

## Current State
- 92 tests passing (was 81, +11 onboarding tests)
- TypeScript compiles clean, Vite builds clean
- All 6 features from sprint plan implemented
- Backend API: health, patients (GET list, GET detail, POST create), chat (text/audio/image)
- Frontend: PatientView (voice-first + image), ClinicianView (responsive 3-layout), OnboardingForm, MetricTooltip

## Tomorrow's Plan
- End-to-end manual testing with running backend + frontend
- Test voice quality with actual TTS output
- Test onboarding flow with real patient creation
- Test image intake with medication bottle photo
- Test mobile layout on actual phone/iPad
- Consider adding more test coverage for image service (mocked)

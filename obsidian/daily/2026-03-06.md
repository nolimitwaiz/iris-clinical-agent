# 2026-03-06

## What Was Built Today

### Morning (Vision/Docs)
- Rewrote SOUL.md from technical spec to manifesto
- Updated CLAUDE.md with new technical sections: voice biomarker pipeline, visual assessment pipeline, behavioral state machine, 4-layer patient state, proactive outreach, digital twin lite, clinical roadmap
- Created architecture and decision log notes

### Afternoon (Implementation: "Less, but Perfect")
- **Complete GDMT Engine**: Added 3 new evaluators to `src/tools/gdmt_engine.py`:
  - `_evaluate_arni_acei_arb()` — RAAS pillar with ACEi-to-ARNI switching, 36h washout, ARB direct switch, ARNI uptitration (24→49→97mg)
  - `_evaluate_mra()` — Spironolactone initiation/titration (12.5→25→50mg), K+ safety gates (>=5.0 hold, >=5.5 stop), eGFR<30 avoidance
  - `_evaluate_sglt2i()` — Dapagliflozin 10mg single dose, eGFR>=20 gate, type 1 diabetes contraindication, can continue if already on it below eGFR 20
  - `evaluate_gdmt()` now returns 5 packets: [diuretic, BB, ARNI, MRA, SGLT2i]

- **Medication Optimization Score**: New `src/utils/mos.py`
  - 0-100 score across 4 GDMT pillars (25 points each)
  - Contraindicated pillars get full credit (don't penalize patients)
  - Score = (current_dose / target_dose) * 25 per pillar

- **Composite Decompensation Risk Score**: Added `_compute_composite_risk()` to `src/tools/trajectory_analyzer.py`
  - 5 weighted sub-scores: weight trend (0.35), BP (0.20), HR (0.15), adherence (0.15), BNP (0.15)
  - Tiers: low (0-25), moderate (26-50), high (51-70), critical (71-100)
  - Attached as `risk_score` dict on trajectory packet

- **API Integration**: Updated `api/schemas.py` with PillarScore, MOSResponse, RiskComponent, RiskScore models. Updated all 4 pipeline service return paths to include MOS.

- **Frontend Components**: 3 new React components:
  - `MOSGauge.tsx` — SVG circular progress ring with pillar breakdowns
  - `RiskGauge.tsx` — Large number + tier badge + component bars
  - `GDMTCard.tsx` — 4 pillar progress bars with active recommendation badges
  - Integrated into PatientDashboard (hero metrics row) and ClinicianView (GDMT card in clinical panel)

- **Tests**: 24 new tests across 3 files
  - `test_gdmt.py`: TestARNI (5), TestMRA (4), TestSGLT2i (4) classes
  - `test_mos.py`: 4 tests (low/moderate MOS, all at target, structure)
  - `test_trajectory.py`: 6 risk score tests (presence, low/moderate/critical, component sum, components present)

## Key Decisions Made
- Cut voice biomarkers, edema grading, behavioral engine, proactive outreach — all V2
- Focus on 2 hero metrics (MOS + Risk Score) that tell a patient's story in 10 seconds
- GDMT packets index: 0=diuretic, 1=BB, 2=ARNI, 3=MRA, 4=SGLT2i (stable ordering)
- MOS gives contraindicated pillars full credit to avoid penalizing patients
- Risk score uses simple step function scoring (not continuous) for interpretability

## Problems Encountered
- f-string format specifier bug with ternary inside `:.0f` — needed nested f-string
- 5 pre-existing safety checker test failures (spironolactone blocking not working for K+>5.0 patients) — not related to this work

## Current State
- All 4 GDMT pillars implemented and tested (was 2/4)
- 115 tests pass, 5 pre-existing failures in safety_checker
- Frontend builds cleanly, TypeScript compiles with no errors
- Demo ready: select patient → see MOS% (red/amber/green) + Risk score → send message → see 5 GDMT recommendations + hero metrics update

### Evening (Architecture Fixes: "Iris Stops Being Stupid")
- **Wired extracted signals into clinical tools** (Task #1 — the #1 reason Iris ignored patients):
  - `check_adherence()` now accepts `signals` — uses `adherence_signals` and `side_effects` to detect non-adherence from patient's own words
  - `analyze_trajectory()` now accepts `signals` — boosts risk level when patient reports decompensation symptoms (edema, dyspnea, etc.)
  - `plan_barriers()` now accepts `signals` — includes `barriers_mentioned` from patient message
  - `run_pipeline()` passes signals through to all three tools

- **Fixed REFERENCE_DATE** (Task #2):
  - Replaced hardcoded `date(2026, 2, 28)` with `date.today()` default
  - Added `reference_date` parameter to `evaluate_gdmt()` and all internal functions
  - All test fixtures pass `reference_date=date(2026, 2, 28)` for determinism
  - Production uses today's date automatically

- **Added onboarding data validation** (Task #3):
  - New `validate_onboarding_data()` in `data_loader.py`
  - Clamps age to 0-120, validates sex (M/F), checks medication names against drug_db
  - Validates insurance tier, truncates long names
  - Integrated into `OnboardingSession.build_patient_data()`

- **Added GDMT recommendation prioritization** (Task #4):
  - New `_prioritize_packets()` in pipeline — assigns priority 1-5 to GDMT decisions
  - stop/hold=1 (always communicate), increase=2, start=3, maintain=4, no_change=5
  - Only top 2 active changes surface per response (MAX_CHANGES_PER_RESPONSE)
  - Deferred changes noted as "additional changes to discuss at next check in"
  - Responder's `_filter_actionable_packets()` respects `communicate_now` flag

- **Fed conversation history into clinical tools** (Task #5):
  - New `_extract_persistent_context()` in pipeline
  - Mines patient turns for barrier keywords (cost, pharmacy distance, transportation, literacy)
  - Mines for adherence keywords (skipping, stopped, ran out)
  - Persistent context merged into signals before tools run
  - "Pharmacy too far" said yesterday persists today without patient repeating it

## Tomorrow's Plan
1. Fix the 5 pre-existing safety checker test failures (spironolactone K+ blocking)
2. End-to-end demo walkthrough verification with API server running
3. Consider adding MOS to streaming SSE events (currently only in done event)
4. Polish: skeleton states before first pipeline run, animation on metric updates
